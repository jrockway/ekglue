apiVersion: v1alpha
cluster_config:
    base:
        connect_timeout: 10s
        type: STRICT_DNS
    overrides:
        - match:
              - cluster_name: www:www-jrock-us:h2
          override:
              http2_protocol_options: {}
